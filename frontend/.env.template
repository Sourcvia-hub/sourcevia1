# ProcureFlix Frontend Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# Backend API URL (OPTIONAL)
# ============================================================================
# NOTE: When using Docker Compose, this variable is OPTIONAL because nginx 
# automatically proxies /api/ requests to the backend service.
#
# Only set this if:
# 1. Running frontend in development mode (yarn start)
# 2. Using a separate backend deployment (different server/domain)
#
# For local development with Docker:
# REACT_APP_BACKEND_URL=http://localhost:8001
#
# For production with separate backend:
# REACT_APP_BACKEND_URL=https://api.your-domain.com
#
# For Docker Compose deployment: Leave empty or comment out
# REACT_APP_BACKEND_URL=

# ============================================================================
# Application Settings
# ============================================================================
REACT_APP_NAME=ProcureFlix
REACT_APP_VERSION=1.0.0
